    @model IEnumerable<BoardRequestsViewModel>

<!DOCTYPE html>
<html lang="en" dir="rtl" data-theme="light">
<body>
    <section class="page_wrap">
        <div class="page_content">
            <div class="block mb-4">
                <div class="block-header border-bottom px-4 py-3">
                    <h5>تفاصيل صاحب الطلب</h5>
                    <p><strong>الاسم:</strong> @User.FindFirst(ClaimTypes.GivenName)?.Value</p>
                    <p><strong>البريد الالكتروني:</strong> @User.FindFirst(ClaimTypes.Email)?.Value</p>
                    <p><strong>رقم الجوال:</strong> @User.FindFirst(ClaimTypes.MobilePhone)?.Value</p>
                </div>
                <div class="block-body py-5">
                    <div class="card">
                        <div class="card-header">
                            <h5 class="m-0">تفاصيل الفعالية</h5>
                        </div>
                    </div>
                </div>
                <div class="block-body">
                    <table class="table table-hover js-dataTable">
                        <thead>
                            <tr>
                                <th>@Html.DisplayNameFor(m => m.FullNameAr)</th>
                                <th>@Html.DisplayNameFor(m => m.BoardId)</th>
                                <th>@Html.DisplayNameFor(m => m.RequestStatusId)</th>
                                <th>@Html.DisplayNameFor(m => m.RejectionReasons)</th>
                                <th>الإجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var request in Model)
                            {
                                <tr >
                                    
                                    <td>@request.FullNameAr</td>
                                    <td>@request.BoardName</td>
                                    <td style="@(request.RequestStatusId == Guid.Parse("59A1AE40-BF57-48AA-BF63-7672B679C152") ? "background-color: rgb(15, 181, 21); color: white; border-radius: 15px; padding: 10px; text-align: center;" : "padding: 10px; text-align: center;")">@request.RequestStatusName</td>
                                    <td>@request.RejectionReasons</td>
                                    <td>
                                        <form asp-action="Details">
                                            <input type="hidden" name="id" value="@request.Id" />
                                            <button type="submit" class="btn" style="background-color: rgb(15, 113, 121); color: white;">تفاصيل الطلب</button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section><!-- End Page Wrap -->
</body>
</html>